package com.multitv.eventbuilder.ui.roomdb.model

/*@Entity(tableName = "notifications")
data class NotificationEntity(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val title: String,
    val message: String,
    val imageUrl: String?,
    val timestamp: Long = System.currentTimeMillis()
)*/
